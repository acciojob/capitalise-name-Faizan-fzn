//your JS code here. If required.
   document.querySelector('#fname').addEventListener('change', capital)

function capital(event){
       // var nameInput = fname.value;
    // var uppercaseName = nameInput.toUpperCase();
    fname.value = fname.value.toUpperCase();; 
}